<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title><%= data.name %>の詳細</title>
    <style>
        /* --- 基本設定 --- */
        body {
            background-color: #ffffff;
            color: #333333;
            font-family: "Yu Mincho", "Hiragino Mincho ProN", serif;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 700px;
            margin: 0 auto;
            border: 1px solid #cccccc;
            padding: 30px;
            background-color: #ffffff;
        }

        /* --- 1. クリーチャー名 --- */
        h1 {
            color: #333333;
            border-bottom: 2px solid #333333;
            padding-bottom: 10px;
            margin-bottom: 20px;
            font-size: 2em;
        }

        /* --- 2. 表のデザイン --- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            font-size: 1.1em;
        }

        th, td {
            border: 1px solid #999999;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f5f5f5;
            width: 25%;
            font-weight: normal;
        }

        td {
            background-color: #ffffff;
        }

        /* --- ボタン類 --- */
        a { color: #0066cc; text-decoration: none; }
        a:hover { text-decoration: underline; }
        
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background: #eeeeee;
            color: #333;
            border: 1px solid #999;
            margin: 5px;
            cursor: pointer;
            font-size: 0.9em;
        }
        .btn:hover { background-color: #dddddd; }
        
        .btn-delete {
            background-color: #fff0f0;
            color: #cc0000;
            border-color: #cc0000;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1><%= data.name %></h1>

        <table>
            <tr>
                <th>カード名</th>
                <td><%= data.name %></td>
            </tr>
            <tr>
                <th>種類</th>
                <td><%= data.element %>属性</td>
            </tr>
            <tr>
                <th>コスト</th>
                <td><%= data.cost %></td>
            </tr>
            <tr>
                <th>ST</th>
                <td><%= data.st %></td>
            </tr>
            <tr>
                <th>HP</th>
                <td><%= data.hp %></td>
            </tr>
            <tr>
                <th>能力</th>
                <td><%= data.ability %></td>
            </tr>
            <tr>
                <th>特記事項</th>
                <td style="white-space: pre-wrap;"><%= data.memo %></td>
            </tr>
            <tr>
                <th>解説</th>
                <td><%= data.description %></td>
            </tr>
            <tr>
                <th>使用メモ</th>
                <td style="white-space: pre-wrap; color: #555;"><%= data.usage_note %></td>
            </tr>
        </table>

        <div>
            <a href="/creatures/edit/<%= data.id %>" class="btn">編集</a>
            
            <form action="/creatures/delete/<%= data.id %>" method="POST" style="display:inline;" onsubmit="return confirm('本当に削除しますか？');">
                <input type="submit" value="消去" class="btn btn-delete">
            </form>
        </div>

        <p style="margin-top: 20px;">
            <a href="/creatures">一覧に戻る</a>
        </p>
    </div>

</body>
</html>